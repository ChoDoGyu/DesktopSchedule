<UserControl x:Class="DesktopSchedule.Views.WeeklyScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:DesktopSchedule.Controls"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="1000">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Grid x:Name="DragSurface"
          PreviewMouseMove="DragSurface_PreviewMouseMove"
          PreviewMouseLeftButtonUp="DragSurface_PreviewMouseLeftButtonUp"
          PreviewKeyDown="DragSurface_PreviewKeyDown"
          LostMouseCapture="DragSurface_LostMouseCapture">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"
                  Margin="0,0,0,8">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Content="&lt;"
                        Command="{Binding PreviousWeekCommand}"
                        Width="36"
                        Height="30" />

                <TextBlock Grid.Column="1"
                           Text="{Binding WeekRangeText}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontSize="16"
                           FontWeight="SemiBold" />

                <Button Grid.Column="2"
                        Content="&gt;"
                        Command="{Binding NextWeekCommand}"
                        Width="36"
                        Height="30" />
            </Grid>

            <Grid Grid.Row="1"
                  Margin="0,0,0,10">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <CheckBox Grid.Column="0"
                          Content="완료 일정 보기"
                          IsChecked="{Binding ShowCompletedSchedules}"
                          VerticalAlignment="Center"
                          HorizontalAlignment="Left" />

                <Button Grid.Column="1"
                        Content="이번 주"
                        Command="{Binding CurrentWeekCommand}"
                        Padding="12,5" />

                <Button Grid.Column="2"
                        Content="+ 일정"
                        Command="{Binding OpenNewScheduleCommand}"
                        HorizontalAlignment="Right"
                        Padding="12,5" />
            </Grid>

            <Grid Grid.Row="2">

                <Grid>
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="Visibility"
                                    Value="Visible" />

                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsEditorOpen}"
                                             Value="True">
                                    <Setter Property="Visibility"
                                            Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>

                    <ScrollViewer HorizontalScrollBarVisibility="Auto"
                                  VerticalScrollBarVisibility="Auto">

                        <Grid x:Name="WeekCalendarArea"
                              Width="980"
                              MinHeight="380">

                            <Grid.RowDefinitions>
                                <RowDefinition Height="42" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <ItemsControl Grid.Row="0"
                                          ItemsSource="{Binding Days}">

                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Columns="7" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>

                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Button Command="{Binding DataContext.OpenNewScheduleForDateCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Height="42"
                                                BorderThickness="0,0,1,1"
                                                BorderBrush="LightGray"
                                                Padding="4">

                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Background"
                                                            Value="Transparent" />

                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsToday}"
                                                                     Value="True">
                                                            <Setter Property="FontWeight"
                                                                    Value="Bold" />
                                                        </DataTrigger>

                                                        <DataTrigger Binding="{Binding IsSelected}"
                                                                     Value="True">
                                                            <Setter Property="Background"
                                                                    Value="Gainsboro" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>

                                            <StackPanel Orientation="Horizontal"
                                                        HorizontalAlignment="Center">

                                                <TextBlock Text="{Binding DayName}"
                                                           FontSize="15" />

                                                <TextBlock Text="{Binding DayText}"
                                                           Margin="6,0,0,0"
                                                           FontSize="15" />
                                            </StackPanel>
                                        </Button>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <Grid Grid.Row="1"
                                  MinHeight="338">

                                <Grid.RowDefinitions>
                                    <RowDefinition Height="{Binding SpanningAreaHeight}" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>

                                <ItemsControl Grid.RowSpan="2"
                                              ItemsSource="{Binding Days}"
                                              Panel.ZIndex="0"
                                              IsHitTestVisible="False">

                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <UniformGrid Columns="7" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>

                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border BorderBrush="LightGray"
                                                    BorderThickness="0,0,1,0">

                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background"
                                                                Value="Transparent" />

                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsDropTarget}"
                                                                         Value="True">
                                                                <Setter Property="Background"
                                                                        Value="AliceBlue" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <ItemsControl Grid.Row="0"
                                              ItemsSource="{Binding SpanningSchedules}"
                                              Width="980"
                                              Height="{Binding SpanningAreaHeight}"
                                              VerticalAlignment="Top"
                                              Panel.ZIndex="2">

                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <Canvas Width="980" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>

                                    <ItemsControl.ItemContainerStyle>
                                        <Style TargetType="ContentPresenter">
                                            <Setter Property="Canvas.Left"
                                                    Value="{Binding Left}" />

                                            <Setter Property="Canvas.Top"
                                                    Value="{Binding Top}" />
                                        </Style>
                                    </ItemsControl.ItemContainerStyle>

                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Button Width="{Binding Width}"
                                                    Height="{Binding Height}"
                                                    HorizontalContentAlignment="Stretch"
                                                    Padding="6,2"
                                                    ToolTip="{Binding ToolTipText}"
                                                    PreviewMouseLeftButtonDown="ScheduleButton_PreviewMouseLeftButtonDown">

                                                <Button.Style>
                                                    <Style TargetType="Button">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsCompleted}"
                                                                         Value="True">
                                                                <Setter Property="Background"
                                                                        Value="#E7E7E7" />
                                                                <Setter Property="BorderBrush"
                                                                        Value="#BDBDBD" />
                                                                <Setter Property="Opacity"
                                                                        Value="0.58" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>

                                                <TextBlock Text="{Binding DisplayText}"
                                                           FontWeight="SemiBold"
                                                           TextTrimming="CharacterEllipsis"
                                                           TextWrapping="NoWrap">

                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsCompleted}"
                                                                             Value="True">
                                                                    <Setter Property="Foreground"
                                                                            Value="#666666" />
                                                                    <Setter Property="TextDecorations"
                                                                            Value="Strikethrough" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Button>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <ItemsControl Grid.Row="1"
                                              ItemsSource="{Binding Days}"
                                              MinHeight="300"
                                              Panel.ZIndex="1">

                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <UniformGrid Columns="7" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>

                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid MinWidth="140"
                                                  Background="Transparent">

                                                <ScrollViewer VerticalScrollBarVisibility="Auto"
                                                              HorizontalScrollBarVisibility="Disabled">

                                                    <ItemsControl ItemsSource="{Binding Schedules}"
                                                                  Margin="4,3,4,8">

                                                        <ItemsControl.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <StackPanel />
                                                            </ItemsPanelTemplate>
                                                        </ItemsControl.ItemsPanel>

                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <Button Height="28"
                                                                        HorizontalContentAlignment="Stretch"
                                                                        Padding="5,2"
                                                                        Margin="0,0,0,3"
                                                                        ToolTip="{Binding ToolTipText}"
                                                                        PreviewMouseLeftButtonDown="ScheduleButton_PreviewMouseLeftButtonDown">

                                                                    <Button.Style>
                                                                        <Style TargetType="Button">
                                                                            <Style.Triggers>
                                                                                <DataTrigger Binding="{Binding IsCompleted}"
                                                                                             Value="True">
                                                                                    <Setter Property="Background"
                                                                                            Value="#EEEEEE" />
                                                                                    <Setter Property="BorderBrush"
                                                                                            Value="#CCCCCC" />
                                                                                    <Setter Property="Opacity"
                                                                                            Value="0.58" />
                                                                                </DataTrigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </Button.Style>

                                                                    <TextBlock Text="{Binding DisplayText}"
                                                                               FontWeight="SemiBold"
                                                                               TextWrapping="NoWrap"
                                                                               TextTrimming="CharacterEllipsis">

                                                                        <TextBlock.Style>
                                                                            <Style TargetType="TextBlock">
                                                                                <Style.Triggers>
                                                                                    <DataTrigger Binding="{Binding IsCompleted}"
                                                                                                 Value="True">
                                                                                        <Setter Property="Foreground"
                                                                                                Value="#666666" />
                                                                                        <Setter Property="TextDecorations"
                                                                                                Value="Strikethrough" />
                                                                                    </DataTrigger>
                                                                                </Style.Triggers>
                                                                            </Style>
                                                                        </TextBlock.Style>
                                                                    </TextBlock>
                                                                </Button>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>
                                                </ScrollViewer>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                            </Grid>
                        </Grid>
                    </ScrollViewer>
                </Grid>

                <Border BorderBrush="LightGray"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="12"
                        Visibility="{Binding IsEditorOpen, Converter={StaticResource BooleanToVisibilityConverter}}">

                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">

                        <StackPanel>

                            <TextBlock Text="{Binding EditorTitle}"
                                       FontSize="20"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,12" />

                            <TextBlock Text="제목"
                                       Margin="0,0,0,4" />

                            <TextBox Text="{Binding NewTitle, UpdateSourceTrigger=PropertyChanged}"
                                     Padding="6"
                                     Margin="0,0,0,10" />

                            <TextBlock Text="설명"
                                       Margin="0,0,0,4" />

                            <TextBox Text="{Binding NewDescription, UpdateSourceTrigger=PropertyChanged}"
                                     Height="70"
                                     AcceptsReturn="True"
                                     TextWrapping="Wrap"
                                     VerticalScrollBarVisibility="Auto"
                                     Padding="6"
                                     Margin="0,0,0,10" />

                            <TextBlock Text="시작"
                                       Margin="0,0,0,4" />

                            <Grid Margin="0,0,0,10">

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="8" />
                                    <ColumnDefinition Width="90" />
                                </Grid.ColumnDefinitions>

                                <controls:ScheduleDatePicker Grid.Column="0"
                                                             SelectedDate="{Binding NewStartDate}" />

                                <TextBox Grid.Column="2"
                                         Text="{Binding NewStartTime, UpdateSourceTrigger=PropertyChanged}"
                                         Padding="6" />
                            </Grid>

                            <TextBlock Text="종료"
                                       Margin="0,0,0,4" />

                            <Grid Margin="0,0,0,10">

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="8" />
                                    <ColumnDefinition Width="90" />
                                </Grid.ColumnDefinitions>

                                <controls:ScheduleDatePicker Grid.Column="0"
                                                             SelectedDate="{Binding NewEndDate}" />

                                <TextBox Grid.Column="2"
                                         Text="{Binding NewEndTime, UpdateSourceTrigger=PropertyChanged}"
                                         Padding="6" />
                            </Grid>

                            <CheckBox Content="하루 종일"
                                      IsChecked="{Binding NewIsAllDay}"
                                      Margin="0,0,0,10" />

                            <CheckBox Content="알림 사용"
                                      IsChecked="{Binding NewIsReminderEnabled}"
                                      Margin="0,0,0,8" />

                            <StackPanel Orientation="Horizontal"
                                        Margin="0,0,0,10">

                                <ComboBox ItemsSource="{Binding ReminderMinuteOptions}"
                                          SelectedItem="{Binding NewReminderMinutesBefore}"
                                          IsEnabled="{Binding NewIsReminderEnabled}"
                                          Width="100" />

                                <TextBlock Text="분 전 알림"
                                           VerticalAlignment="Center"
                                           Margin="8,0,0,0" />
                            </StackPanel>

                            <TextBlock Text="{Binding ErrorMessage}"
                                       TextWrapping="Wrap"
                                       Margin="0,0,0,10" />

                            <WrapPanel HorizontalAlignment="Right">

                                <Button Content="완료"
                                        Command="{Binding MarkAsCompletedCommand}"
                                        Padding="12,6"
                                        Margin="0,0,6,0">

                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Visibility"
                                                    Value="Collapsed" />

                                            <Style.Triggers>
                                                <MultiDataTrigger>
                                                    <MultiDataTrigger.Conditions>
                                                        <Condition Binding="{Binding IsEditMode}"
                                                                   Value="True" />

                                                        <Condition Binding="{Binding IsEditingCompleted}"
                                                                   Value="False" />
                                                    </MultiDataTrigger.Conditions>

                                                    <Setter Property="Visibility"
                                                            Value="Visible" />
                                                </MultiDataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <Button Content="완료 취소"
                                        Command="{Binding MarkAsIncompleteCommand}"
                                        Padding="12,6"
                                        Margin="0,0,6,0">

                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Visibility"
                                                    Value="Collapsed" />

                                            <Style.Triggers>
                                                <MultiDataTrigger>
                                                    <MultiDataTrigger.Conditions>
                                                        <Condition Binding="{Binding IsEditMode}"
                                                                   Value="True" />

                                                        <Condition Binding="{Binding IsEditingCompleted}"
                                                                   Value="True" />
                                                    </MultiDataTrigger.Conditions>

                                                    <Setter Property="Visibility"
                                                            Value="Visible" />
                                                </MultiDataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <Button Content="삭제"
                                        Command="{Binding DeleteScheduleCommand}"
                                        Padding="12,6"
                                        Margin="0,0,6,0">

                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Visibility"
                                                    Value="Collapsed" />

                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsEditMode}"
                                                             Value="True">
                                                    <Setter Property="Visibility"
                                                            Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <Button Content="취소"
                                        Command="{Binding CancelEditCommand}"
                                        Padding="12,6"
                                        Margin="0,0,6,0" />

                                <Button Content="저장"
                                        Command="{Binding SaveScheduleCommand}"
                                        Padding="12,6" />

                            </WrapPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Grid>

        <Canvas IsHitTestVisible="False"
                Panel.ZIndex="1000">

            <controls:ScheduleDragPreview x:Name="DragPreview" />
        </Canvas>
    </Grid>
</UserControl>