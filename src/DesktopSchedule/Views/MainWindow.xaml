<Window x:Class="DesktopSchedule.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:DesktopSchedule.ViewModels"
        xmlns:views="clr-namespace:DesktopSchedule.Views"
        mc:Ignorable="d"
        Title="{Binding Title}"
        Width="1120"
        Height="960"
        MinWidth="900"
        MinHeight="800"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen"
        Topmost="{Binding IsTopmost}"
        ShowInTaskbar="{Binding ShowInTaskbar}">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:MonthlyCalendarViewModel}">
            <views:MonthlyCalendarView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:WeeklyScheduleViewModel}">
            <views:WeeklyScheduleView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:TodoViewModel}">
            <views:TodoView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <views:SettingsView />
        </DataTemplate>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{Binding Title}"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Margin="0,0,0,16" />

        <UniformGrid Grid.Row="1"
                     Rows="1"
                     Columns="4"
                     Margin="0,0,0,16">

            <Button Content="월간"
                    Command="{Binding ShowMonthlyCalendarCommand}"
                    Margin="0,0,4,0"
                    Padding="6" />

            <Button Content="주간"
                    Command="{Binding ShowWeeklyScheduleCommand}"
                    Margin="4,0"
                    Padding="6" />

            <Button Content="할 일"
                    Command="{Binding ShowTodoCommand}"
                    Margin="4,0"
                    Padding="6" />

            <Button Content="설정"
                    Command="{Binding ShowSettingsCommand}"
                    Margin="4,0,0,0"
                    Padding="6" />
        </UniformGrid>

        <Border Grid.Row="2"
                BorderBrush="LightGray"
                BorderThickness="1"
                CornerRadius="8"
                Padding="12">

            <ContentControl Content="{Binding CurrentViewModel}" />
        </Border>
    </Grid>
</Window>